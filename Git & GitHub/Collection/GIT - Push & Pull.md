## Connect to GitHub and Push & Pull
```bash
git remote add origin <link> # Connect to GitHub (only once)
git push -u origin main      # Push to GitHub (first push)

git push                     # Later pushes
git pull                     # Pull latest changes
```