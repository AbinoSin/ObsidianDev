```bash
git config --global user.email "someone@email.com"
git config --global user.name "YourName"
```

```bash
git config --list
```

---

>[!info] Changing the editor from VIM to Code !!
>>[!example] Step 1
>>- One VsCode and set command line tool in it by:
>>- Searching this line ==Shell Command: Install 'code' command in PATH==
>
>>[!example] Step 2
>>Run this command
>>```bash
>>git config --global core.editor "code --wait"
>>```





