>[!summary] PROPERTIES
>- `Math.PI`
>- `Math.E`
>- More

>[!summary] METHODS
>```js
>Math.abs(n)     // absolute
>Math.pow(a, b)  // power
>Math.floor(n)   // round-off smaller
>Math.ceil(n)    // round-off larger
>Math.random()   // 0 to 1 random value, but never 1 (its inclusive)
>// more
>```
